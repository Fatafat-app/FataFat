export interface User {
  id: string;
  name: string;
  email: string;
}

export interface Restaurant {
  id: string;
  name: string;
  address: string;
}

export interface Food {
  id: string;
  name: string;
  price: number;
}
